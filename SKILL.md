---
name: resume-forge
description: 把岗位 JD 转化为定制的一页 A4 简历(HTML+PDF):候选人档案匹配 + 诚实基线 + STAR chip 排版 + 无头 Chrome 自动导出与内容级验证 + 多版本矩阵管理。当用户发来 JD 要求定制简历,或要求改写/导出/校验/统一管理多版本简历时使用。
---

# resume-forge —— 一页 A4 定制简历生产线

把"岗位 JD"锻造成"一页 A4 PDF 简历"的完整流水线,支持多版本矩阵管理与批量重导。

## 零、前置条件

1. **候选人档案** `profile/profile.md`(按 `profile/profile.example.md` 的格式填写):
   - 基本信息 / 教育 / 实习 / 项目池(每个项目附**诚实边界**)/ 技能池 / 获奖
   - **诚实基线铁律区**:官方 GPA 等数字口径、未收敛项目清单、禁写词清单——每次产出简历前逐条核对
2. **模板** `template/resume_template.html`(主题色、chip 标签、版式见模板注释)
3. **工具** `scripts/export_autofit.py`(导出+自动压页)、`scripts/verify_pdf.py`(内容级验证)

## 一、单份定制流程(收到 JD 后执行)

1. **解析 JD**:核心职责 / 任职要求 / 关键词 / 加分项 / 专业限制 / 届别。
2. **匹配档案**:从档案挑对口素材;逐一识别 gap——**gap 一律不编造**(对照诚实基线)。
3. **定位**:①求职意向 ②主打经历(哪段实习/项目 lead)③技术栈排序 ④主题色(与已有版本**不撞色**,维护一张"版本-主色"表)。
4. **选项目**:按方向从项目池选 2 个最对口的;经历不足时宁缺毋滥。
5. **STAR 写作**(规范见「二」):每条 = 背景/方法/结果,量化优先。
6. **生成 HTML**:复用模板,文件名 `<姓名>简历_<版本名>.html`,保证一页。
7. **导出 PDF**:`python scripts/export_autofit.py <html>`——自动处理 URL 编码、溢出压页。
8. **内容级验证**:`python scripts/verify_pdf.py <pdf>`——页数/尺寸/嵌入图片(⚠️ 只查页数会被错误页骗过)。
9. **汇报**:①JD 关键词命中表 ②诚实边界(没编的 gap)③面试 defense 提醒(每个数字能讲出口径)。

## 二、写作规范(STAR chip 格式)

- 每条经历 bullet = **加粗标题** + 三个主题色小标签 + 分号分段:

```html
<li><strong>经历标题</strong>：<span class="tech-tag">背景</span>为什么做/要达成什么；
<span class="tech-tag">方法</span>动词开头,具体怎么做(带技术栈与真实决策)；
<span class="tech-tag">结果</span>必须量化(次数/小时/效率/规模),数字用 highlight</li>
```

- **禁忌(血泪教训)**:
  - ❌ 教科书标准结构展开当"专业细节"(如把条件 UNet 括号成"通道拼接注入、编码器-瓶颈-解码器+跳连")——内行一眼识破、AI 味浓;专业度由**具体参数、自建数据、真实工作量**体现
  - ❌ 未收敛项目报精度/写"求解成功";区分**定性验证**(如现象观测)与**定量验证**(测量数据),措辞不得越界
  - ❌ 复数来源的数字混写口径(设备·小时≠单机小时;"相同模型"对比不得写"实测")
  - ❌ 凑术语、无信息量的排比列举
- 英文简历不加中文标签,按英文惯例写。

## 三、模板规则

- A4:`@page { size: A4; margin: 0.85-1cm 1.2-1.4cm; }`,正文 9pt,字体 `'Noto Sans SC','Microsoft YaHei'`(网络字体不可用时自动回落雅黑,排版不受影响)
- 结构:求职意向条(主色底白字)→ 姓名+联系方式+照片 → 教育/实习/项目/技能/获奖(顺序按岗位调)
- 元素:技术栈标签 `tech-tag`(兼作 STAR chip)、量化高亮 `highlight`
- **必须一页**;溢出先收行距/边距,再 zoom(见四)

## 四、PDF 导出(无头 Chrome 的坑,全部踩过)

```bash
# URL 必须百分号编码!中文路径不编码 → Chrome 把 ERR_FILE_NOT_FOUND 错误页打印进 PDF(≈39KB 假文件)
python scripts/export_autofit.py 简历_某版本.html   # 已内置编码+压页
```

- **必须用独立 `--user-data-dir`**(Windows 风格路径,勿用 POSIX /tmp),否则吸附已运行的 Chrome 报 0x5
- **必须加 `--virtual-time-budget=15000`**:无头打印可能在 Google Fonts 异步加载完成前就截图,导致 PDF 回落本地字体(雅黑/Arial);加此参数等待字体就绪(2026-08-23 实测:不加→嵌入 MicrosoftYaHei,加→嵌入 NotoSansSC)
- 输出先写 ASCII 临时名再 `mv -f` 替换(目标被查看器占用时尤其重要);传给 `--print-to-pdf` 的路径**必须是绝对路径**(Chrome 把相对路径解析到自身安装目录)
- **溢出压页**:注入 `/*AUTO-FIT*/ .resume{zoom:X}`,X 从 0.97 逐级下探(0.95/0.93/0.91/0.89/0.87/0.85)直到恰好一页;zoom < 0.85 仍溢出时改收行距/边距(密度优先于缩字)
- **验证必须内容级**:真实简历通常 ≥150KB 且含嵌入图片(`/Subtype /Image`);错误页恒 ≈39KB 且也是"1 页"——**只查页数必被欺骗**(2026-08-22 事故:19 个 PDF 全是错误页,两轮"验证通过"都是假的)

## 五、多版本矩阵管理

- 命名统一:`<姓名>简历_<版本名>.html/.pdf`(如 车载软件测试版 / 算法岗版 / 英文版)
- 每版差异化:求职意向、主色、经历侧重、项目选择——**不要一份通投**
- **经历评审清单工作流**(适合大改):
  1. 生成 `经历评审清单.md`:把所有版本的经历去重分组编号(A1/A2…),标注使用版本与诚实风险(🔴违规/🟡不一致)
  2. 用户逐条给意见(保留/删除/改写),或补充新素材
  3. 定稿写入清单(含"已确认事项"),再**统一应用**到全部版本(内容相同的替换可用脚本批量做,措辞不同的逐文件处理)
  4. 全量重导 + 终验,清单记录应用结果
- 诚实口径升级时(如"自研台架"实为公司资产),**全版本统一修正**,并同步回候选人档案
- git 不跟踪导出的 PDF 时注意:用户常用查看器打开 PDF,批量覆盖前提醒关闭

## 六、技能安装

```
# 用户级(所有项目可用)
cp -r resume-forge ~/.claude/skills/resume-forge
# 或项目级
cp -r resume-forge <项目>/.claude/skills/resume-forge
```

安装后把候选人档案填到 `profile/profile.md`,对 Claude 说"按这个 JD 改简历"即可触发。
